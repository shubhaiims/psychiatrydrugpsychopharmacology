{
  "name": "psychopharmacology-drug-library",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node server/index.js",
    "start": "node server/index.js",
    "build": "node --check app.js && node --check server/index.js && node scripts/validate.mjs",
    "test": "npm run build"
  }
}
